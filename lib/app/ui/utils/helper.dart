import 'package:flutter/material.dart';

class Helper {
  static const radius20 = Radius.circular(20);
  static const all20 = BorderRadius.all(Radius.circular(20));
  static const all10 = BorderRadius.all(Radius.circular(10));
  static const all15 = BorderRadius.all(Radius.circular(15));
  static const all6 = BorderRadius.all(Radius.circular(6));
  static const radius16 = Radius.circular(16);

  static const double xxs = 0.5;
  static const double xs = 1;
  static const double s = 2;
  static const double m = 4;
  static const double l = 8;
  static const double xl = 16;
  static const double xxl = 32;
  static const double xxxl = 48;
}
